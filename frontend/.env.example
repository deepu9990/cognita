# Leave blank in development so requests go to /api on this origin and the
# Vite proxy forwards them, which keeps auth cookies first-party.
# Set it to the backend URL only for a production build on the same site.
VITE_API_URL=

# Where the dev proxy forwards /api requests.
VITE_PROXY_TARGET=http://localhost:5000
